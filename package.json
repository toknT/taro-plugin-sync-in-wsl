{
  "name": "taro-plugin-sync-in-wsl",
  "version": "1.0.0",
  "description": "sync files in wsl2's filesystem to windows's filesystem",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "taro",
    "plugin",
    "wsl"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tarojs/service": "^3.0.8"
  },
  "devDependencies": {
    "shelljs": "^0.8.4",
    "typescript": "^4.0.2"
  }
}
